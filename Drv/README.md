# Drv Layer

This layer owns low-level peripheral drivers and interrupt-facing code.

It may use StdPeriph and direct register access.
It must remain simple, deterministic, and free of application policy.
